# Memento

A warm, tactile React Native memory journal built with Expo. Browse saved moments, filter the archive, and move between the primary app sections from a translucent navigation bar.

## Run locally

```bash
npm install
npm start
```

Use `npm run ios`, `npm run android`, or `npm run web` to open a specific platform.
